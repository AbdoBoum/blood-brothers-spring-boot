eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_SERVER:http://discovery:8761/eureka}
spring:
  application:
    name: acceuil-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIGSERVER
